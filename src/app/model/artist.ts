export interface Artist {
    _id: string
    name: string
    photoUrl?: string,
    birthdate?: string,
    deathDate?: number,
    genre?: string,
    _createdAt?: string,
    _updatedAt?: string,
}
